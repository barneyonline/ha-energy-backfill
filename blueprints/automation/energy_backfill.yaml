blueprint:
  name: Energy Backfill
  description: >-
    Convert a delayed daily energy report into a lifetime counter and backfill
    long-term statistics for a proper energy sensor while tracking active cycle
    durations for per-cycle attribution and optional power statistics.
  domain: automation
  input:
    energy_yesterday_sensor:
      name: Energy Yesterday Sensor
      description: LG ThinQ "energy yesterday" entity.
      selector:
        entity:
          domain: sensor
    energy_yesterday_unit:
      name: Energy Yesterday Unit
      description: Unit for the energy yesterday sensor value.
      default: Wh
      selector:
        select:
          options:
            - Wh
            - kWh
          mode: dropdown
    status_sensor:
      name: Status Sensor
      description: LG ThinQ status entity used to detect active cycles.
      selector:
        entity:
          domain: sensor
    lifetime_energy_helper:
      name: Lifetime Energy Helper
      description: input_number holding cumulative kWh.
      selector:
        entity:
          domain: input_number
    lifetime_energy_statistic_id:
      name: Lifetime Energy Sensor
      description: >-
        Entity ID of the energy sensor (state_class total_increasing) that will
        receive backfilled statistics. Required.
      selector:
        entity:
          domain: sensor
    power_helper:
      name: Power Helper (Optional)
      description: >-
        Optional input_number helper (W) updated with yesterday's average active
        power for use in a template power sensor.
      default: ""
      selector:
        entity:
          domain: input_number
    power_statistic_id:
      name: Power Sensor (Optional)
      description: >-
        Optional power sensor entity ID (state_class measurement) that will
        receive backfilled mean statistics in W.
      default: ""
      selector:
        entity:
          domain: sensor
    cycle_start_helper:
      name: Cycle Start Helper
      description: input_datetime tracking the current cycle start timestamp.
      selector:
        entity:
          domain: input_datetime
    daily_active_seconds_helper:
      name: Daily Active Seconds Helper
      description: input_number storing total active seconds for the day.
      selector:
        entity:
          domain: input_number
    cycle_durations_helper:
      name: Cycle Durations Helper
      description: input_text with JSON array of cycle durations (seconds).
      selector:
        entity:
          domain: input_text
    last_processed_date_helper:
      name: Last Processed Date Helper
      description: input_text storing the last processed date (YYYY-MM-DD).
      selector:
        entity:
          domain: input_text
    inactive_states:
      name: Inactive States
      description: States treated as inactive.
      default:
        - off
        - unavailable
        - unknown
      selector:
        object:

trigger:
  - platform: state
    entity_id: !input energy_yesterday_sensor
    id: energy_yesterday
  - platform: state
    entity_id: !input status_sensor
    id: status_change
  - platform: time_pattern
    minutes: 5
    id: hourly_tick
  - platform: homeassistant
    event: start
    id: ha_start

action:
  - variables:
      energy_yesterday_sensor: !input energy_yesterday_sensor
      energy_yesterday_unit: !input energy_yesterday_unit
      status_sensor: !input status_sensor
      lifetime_energy_helper: !input lifetime_energy_helper
      lifetime_energy_statistic_id: !input lifetime_energy_statistic_id
      power_helper: !input power_helper
      power_statistic_id: !input power_statistic_id
      cycle_start_helper: !input cycle_start_helper
      daily_active_seconds_helper: !input daily_active_seconds_helper
      cycle_durations_helper: !input cycle_durations_helper
      last_processed_date_helper: !input last_processed_date_helper
      inactive_states: !input inactive_states
      inactive_states_lower: "{{ inactive_states | map('lower') | list }}"
  - choose:
      - conditions:
          - condition: template
            value_template: >-
              {{ trigger.id in ['energy_yesterday', 'hourly_tick', 'ha_start'] }}
          - condition: template
            value_template: >-
              {% set yesterday_str = (now() - timedelta(days=1)).date().isoformat() %}
              {{ states(last_processed_date_helper) != yesterday_str }}
          - condition: template
            value_template: >-
              {{ states(energy_yesterday_sensor) | is_number }}
          - condition: template
            value_template: >-
              {% set updated = states[energy_yesterday_sensor].last_updated if states[energy_yesterday_sensor] is not none else none %}
              {% set updated_ts = as_timestamp(updated) if updated else none %}
              {% set last_processed_raw = states(last_processed_date_helper) %}
              {% set last_processed_set = last_processed_raw not in ['unknown', 'unavailable', 'none', 'None', ''] %}
              {% set last_processed_midnight = last_processed_raw ~ 'T00:00:00' if last_processed_set else none %}
              {% set last_processed_ts = as_timestamp(last_processed_midnight) if last_processed_midnight else none %}
              {{ trigger.id == 'energy_yesterday' or (updated_ts is not none and (last_processed_ts is none or updated_ts > last_processed_ts)) }}
        sequence:
          - variables:
              yesterday_str: "{{ (now() - timedelta(days=1)).date().isoformat() }}"
              today_midnight_ts: >-
                {{ as_timestamp(now().replace(hour=0, minute=0, second=0, microsecond=0)) }}
              today_midnight_iso: >-
                {{ now().replace(hour=0, minute=0, second=0, microsecond=0).isoformat() }}
              energy_unit: "{{ energy_yesterday_unit | string | lower }}"
              energy_multiplier: >-
                {% if energy_unit == 'kwh' %}
                  1
                {% else %}
                  0.001
                {% endif %}
              status_state: "{{ states(status_sensor) | default('') }}"
              is_active: >-
                {{ status_state | string | lower not in inactive_states_lower }}
              cycle_start_ts: "{{ as_timestamp(states(cycle_start_helper)) }}"
              cycle_start_set: "{{ cycle_start_ts is not none and cycle_start_ts > 0 }}"
              existing_seconds: "{{ states(daily_active_seconds_helper) | float(0) }}"
              daily_active_max: "{{ state_attr(daily_active_seconds_helper, 'max') | float(0) }}"
              durations_raw: "{{ states(cycle_durations_helper) }}"
              durations_safe: >-
                {{ durations_raw if durations_raw not in ['unknown', 'unavailable', 'none', ''] else '[]' }}
              durations_list: >-
                {{ durations_safe | from_json(default=[]) }}
              split_needed: >-
                {{ is_active and cycle_start_set and cycle_start_ts < today_midnight_ts }}
              pre_midnight_sec: >-
                {{ (today_midnight_ts - cycle_start_ts) | int(0) if split_needed else 0 }}
              total_seconds_with_split: "{{ (existing_seconds + pre_midnight_sec) | int(0) }}"
              last_processed_raw: "{{ states(last_processed_date_helper) }}"
              last_processed_set: >-
                {{ last_processed_raw not in ['unknown', 'unavailable', 'none', ''] }}
              no_cycles_recorded: >-
                {{ total_seconds_with_split == 0 and (durations_list | length == 0) }}
              skip_initial_yesterday: "{{ no_cycles_recorded and not last_processed_set }}"
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ split_needed }}"
                sequence:
                  - variables:
                      new_seconds: "{{ existing_seconds + pre_midnight_sec }}"
                      new_seconds_capped: >-
                        {% if daily_active_max > 0 %}
                          {{ [new_seconds, daily_active_max] | min }}
                        {% else %}
                          {{ new_seconds }}
                        {% endif %}
                      new_durations: "{{ durations_list + [pre_midnight_sec] }}"
                  - service: input_number.set_value
                    target:
                      entity_id: !input daily_active_seconds_helper
                    data:
                      value: "{{ new_seconds_capped }}"
                  - service: input_text.set_value
                    target:
                      entity_id: !input cycle_durations_helper
                    data:
                      value: "{{ new_durations | to_json }}"
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: !input cycle_start_helper
                    data:
                      timestamp: "{{ today_midnight_ts }}"
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ skip_initial_yesterday }}"
                sequence:
                  - service: input_text.set_value
                    target:
                      entity_id: !input last_processed_date_helper
                    data:
                      value: "{{ yesterday_str }}"
            default:
              - variables:
                  energy_value: "{{ states(energy_yesterday_sensor) | float(0) }}"
                  energy_kwh: "{{ energy_value * energy_multiplier }}"
                  current_total: "{{ states(lifetime_energy_helper) | float(0) }}"
                  new_total: "{{ current_total + energy_kwh }}"
                  stats_id: "{{ lifetime_energy_statistic_id | string }}"
                  stats_name: "{{ state_attr(stats_id, 'friendly_name') | default(stats_id, true) }}"
                  stats_uom: "{{ state_attr(stats_id, 'unit_of_measurement') | default('kWh', true) }}"
                  average_power_w: >-
                    {% set active_seconds = total_seconds_with_split | float(0) %}
                    {% if active_seconds > 0 %}
                      {{ (energy_kwh * 3600000 / active_seconds) | float(0) }}
                    {% else %}
                      0
                    {% endif %}
                  power_helper_set: "{{ (power_helper | string) not in ['', 'none', 'None'] }}"
                  power_stats_id: "{{ power_statistic_id | string }}"
                  power_stats_set: "{{ power_stats_id not in ['', 'none', 'None'] }}"
                  power_stats_name: "{{ state_attr(power_stats_id, 'friendly_name') | default(power_stats_id, true) }}"
                  power_stats_uom: "{{ state_attr(power_stats_id, 'unit_of_measurement') | default('W', true) }}"
                  stats_start: "{{ today_midnight_iso }}"
              - service: input_number.set_value
                target:
                  entity_id: !input lifetime_energy_helper
                data:
                  value: "{{ new_total }}"
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ power_helper_set }}"
                    sequence:
                      - service: input_number.set_value
                        data:
                          entity_id: "{{ power_helper }}"
                          value: "{{ average_power_w }}"
              - service: recorder.import_statistics
                data:
                  statistic_id: "{{ stats_id }}"
                  source: "recorder"
                  unit_of_measurement: "{{ stats_uom }}"
                  has_mean: false
                  has_sum: true
                  name: "{{ stats_name }}"
                  stats:
                    - start: "{{ stats_start }}"
                      sum: "{{ new_total }}"
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ power_stats_set }}"
                    sequence:
                      - service: recorder.import_statistics
                        data:
                          statistic_id: "{{ power_stats_id }}"
                          source: "recorder"
                          unit_of_measurement: "{{ power_stats_uom }}"
                          has_mean: true
                          has_sum: false
                          name: "{{ power_stats_name }}"
                          stats:
                            - start: "{{ stats_start }}"
                              mean: "{{ average_power_w }}"
              - service: input_number.set_value
                target:
                  entity_id: !input daily_active_seconds_helper
                data:
                  value: 0
              - service: input_text.set_value
                target:
                  entity_id: !input cycle_durations_helper
                data:
                  value: "{{ [] | to_json }}"
              - service: input_text.set_value
                target:
                  entity_id: !input last_processed_date_helper
                data:
                  value: "{{ yesterday_str }}"
      - conditions:
          - condition: trigger
            id: status_change
        sequence:
          - variables:
              status_state: "{{ trigger.to_state.state | default('') }}"
              is_active: "{{ status_state | string | lower not in inactive_states_lower }}"
              cycle_start_ts: "{{ as_timestamp(states(cycle_start_helper)) }}"
              cycle_start_set: "{{ cycle_start_ts is not none and cycle_start_ts > 0 }}"
          - choose:
              - conditions:
                  - condition: template
                    value_template: "{{ is_active and not cycle_start_set }}"
                sequence:
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: !input cycle_start_helper
                    data:
                      timestamp: "{{ as_timestamp(now()) }}"
              - conditions:
                  - condition: template
                    value_template: "{{ (not is_active) and cycle_start_set }}"
                sequence:
                  - variables:
                      now_ts: "{{ as_timestamp(now()) }}"
                      duration_sec: "{{ (now_ts - cycle_start_ts) | int(0) }}"
                      existing_seconds: "{{ states(daily_active_seconds_helper) | float(0) }}"
                      daily_active_max: "{{ state_attr(daily_active_seconds_helper, 'max') | float(0) }}"
                      durations_raw: "{{ states(cycle_durations_helper) }}"
                      durations_safe: >-
                        {{ durations_raw if durations_raw not in ['unknown', 'unavailable', 'none', ''] else '[]' }}
                      durations_list: >-
                        {{ durations_safe | from_json(default=[]) }}
                      new_seconds: "{{ existing_seconds + duration_sec }}"
                      new_seconds_capped: >-
                        {% if daily_active_max > 0 %}
                          {{ [new_seconds, daily_active_max] | min }}
                        {% else %}
                          {{ new_seconds }}
                        {% endif %}
                      new_durations: "{{ durations_list + [duration_sec] }}"
                  - service: input_number.set_value
                    target:
                      entity_id: !input daily_active_seconds_helper
                    data:
                      value: "{{ new_seconds_capped }}"
                  - service: input_text.set_value
                    target:
                      entity_id: !input cycle_durations_helper
                    data:
                      value: "{{ new_durations | to_json }}"
                  - service: input_datetime.set_datetime
                    target:
                      entity_id: !input cycle_start_helper
                    data:
                      timestamp: 0

mode: queued
max_exceeded: silent
